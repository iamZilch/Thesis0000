using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AdjustCameraWhenFlying : MonoBehaviour
{
    // // Start is called before the first frame update
    // GameObject camera;
    // Stage3ScriptHandler scHandler;
    // void Start()
    // {
    //     camera = scHandler.getPlayer().GetComponent<SkillControls>().GetCamera();
    // }

    // // Update is called once per frame
    // private void OnTriggerEnter(Collider other)
    // {
    //     if (other.gameObject.tag == "Zilch" || other.gameObject.tag == "Maze" || other.gameObject.tag == "Trix")
    //     {
    //         other.gameObject.GetComponent<SkillControls>().GetCamera().GetComponent<Camera>().fieldOfView = 80f;
    //         other.gameObject.GetComponent<SkillControls>().GetCamera().GetComponent<Camera>().fieldOfView = +5 * Time.deltaTime;
    //     }
    // }
}
